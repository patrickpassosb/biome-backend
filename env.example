# Biome Coaching Agent - Environment Variables
# Copy this file to .env and fill in your actual values

# ============================================
# BACKEND CONFIGURATION
# ============================================

# Google Gemini API Key (REQUIRED)
# Get your key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your-gemini-api-key-here

# Database Connection (REQUIRED)
# Local development (matches docker-compose-db.yml port mapping):
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/biome_coaching
# Cloud SQL example:
# DATABASE_URL=postgresql://user:password@/biome_coaching?host=/cloudsql/project:region:instance

# Server Configuration
PORT=8080
HOST=0.0.0.0
RELOAD=true
DEBUG=true

# File Upload Configuration
UPLOADS_DIR=uploads
MAX_UPLOAD_SIZE_MB=100

# ============================================
# FRONTEND CONFIGURATION
# ============================================

# API Backend URL
# Local development (if backend runs on different port):
REACT_APP_API_URL=http://localhost:8080
# Production (Cloud Run):
# REACT_APP_API_URL=https://biome-backend-xxxxx.run.app

# ============================================
# CLOUD STORAGE (Optional - for production)
# ============================================

# AWS S3 Configuration (if using S3 for video storage)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret
# S3_BUCKET_NAME=biome-videos
# AWS_REGION=us-east-1

# Google Cloud Storage (recommended for Cloud Run)
# GCS_BUCKET_NAME=biome-videos
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# ============================================
# OPTIONAL CONFIGURATION
# ============================================

# Logging Level (debug, info, warning, error)
LOG_LEVEL=info

# MediaPipe Configuration
MEDIAPIPE_MODEL_COMPLEXITY=1
POSE_DETECTION_FPS=10

# ADK Configuration
ADK_MODEL=gemini-2.0-flash
ADK_TEMPERATURE=0.7

# ============================================
# HACKATHON NOTES
# ============================================
# 
# For local development:
# 1. Create database: psql -U postgres -c "CREATE DATABASE biome_coaching;"
# 2. Run schema: psql -U postgres -d biome_coaching -f schema.sql
# 3. Copy this file: cp env.example .env
# 4. Fill in GOOGLE_API_KEY
# 5. Start backend: python api_server.py
# 6. Start frontend: npm start
#
# For Cloud Run deployment:
# 1. Set environment variables via gcloud run deploy --set-env-vars
# 2. Use Cloud SQL for DATABASE_URL
# 3. Use GCS for video storage
# 4. Set REACT_APP_API_URL to backend Cloud Run URL

